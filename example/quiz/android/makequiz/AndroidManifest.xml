<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="sc.example.quiz.makequiz"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" android:icon="@drawable/icon">
        <provider android:name="sc.example.quiz.content.QuizProvider"
		  android:authorities="sc.example.quiz.content.quizprovider" />
        <activity android:name="QuizList" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="NameQuiz" android:label="@string/app_name" />
        <activity android:name="QuestionList" />
        <activity android:name="EditQuestion" android:theme="@style/TextAppearance.Medium" />
    </application>
</manifest> 
