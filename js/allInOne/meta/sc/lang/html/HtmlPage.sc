/** Javascript needs each top level html object to be marked as MainInit so it generates the .js file for it, including any JS classes not already in libraries.  */
@MainInit(subTypesOnly=true) HtmlPage {
}
